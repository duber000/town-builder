# ConfigMap for non-sensitive configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: town-builder-config
  namespace: town-builder # Make sure this matches your namespace
data:
  # Replace with the actual URL of your external town API
  TOWN_API_URL: "http://your-production-api-url.com/api/towns/"
  # Add other configuration here if needed
