# Secret to store sensitive data like the API token
apiVersion: v1
kind: Secret
metadata:
  name: town-builder-secrets
  namespace: town-builder # Make sure this matches your namespace
type: Opaque
stringData:
  # Replace with your actual JWT token
  TOWN_API_JWT_TOKEN: "YOUR_ACTUAL_API_TOKEN_HERE"
  # Add other secrets here if needed
